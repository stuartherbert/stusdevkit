# Backwards Compatibility

## Introduction

Our aim is to have backwards-compatibility that's similar to the Linux kernel:

- once a public API is in a tagged release, it's supported for the rest of that major version
- bug fixes are allowed to break backwards-compatibility
